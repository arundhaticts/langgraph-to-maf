"""Generated skill plugins (converted from source @tool functions).

Auto-generated by the Framework Conversion Utility. Tool logic is ported from the
source; review any TODO markers.
"""
from __future__ import annotations
{{ imports }}

try:
    from semantic_kernel.functions import kernel_function
except ImportError:  # pragma: no cover - target SDK provided at deploy time
    def kernel_function(*args, **kwargs):
        def _wrap(fn):
            return fn
        return _wrap
{% for tool in tools %}

class {{ tool.class_name }}:
    """Skill converted from source tool '{{ tool.source_name }}'."""

    @kernel_function(description={{ tool.description_repr }})
    def {{ tool.method_name }}(self{{ tool.signature }}){{ tool.return_annotation }}:
{{ tool.body }}
{% endfor %}
