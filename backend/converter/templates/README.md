# Jinja2 Templates (deterministic code generation)

Tier 1 / Tier 2 render output from these templates. Tier 3 `generated_code` is
stitched in alongside them.

Planned (Section 10):

- `plugin_class.py.jinja`   -- `@tool` -> `@kernel_function` plugin class
- `agent_context.py.jinja`  -- state -> MAF context dataclass
- `orchestrator.py.jinja`   -- linear / branch / loop orchestration
- `readme_maf.md.jinja`     -- output README

TODO: author these when the MAF target adapter and Module 7 are implemented.
